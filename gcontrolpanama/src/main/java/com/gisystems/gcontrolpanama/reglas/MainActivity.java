package com.gisystems.gcontrolpanama.reglas;






public class MainActivity {
	
	/* @Override
	    protected void onCreate(Bundle savedInstanceState) {
	        super.onCreate(savedInstanceState);
	        setContentView(R.layout.activity_main);
	 
	        FragmentListaProyectos frgListado
	            =(FragmentListaProyectos)getSupportFragmentManager()
	                .findFragmentById(R.id.FrgListado);
	 
	        frgListado.setProyectosListener(this);
	    }
	 
	    @Override
	    public void onProyectoSeleccionado(Proyecto c) {
	        boolean hayDetalle =
	            (getSupportFragmentManager().findFragmentById(R.id.FrgDetalle) != null);
	 
	        if(hayDetalle) {
	            ((FragmentDetalleProyecto)getSupportFragmentManager()
	                .findFragmentById(R.id.FrgDetalle)).mostrarDetalle(c.getAliasProyecto());
	        }
	        else {
	            Intent i = new Intent(this, DetalleActivity.class);
	            i.putExtra(DetalleActivity.EXTRA_TEXTO, c.getNombreProyecto());
	            startActivity(i);
	        }
	    }*/

}




